# Historical Draft

This file is retained only as historical context from the pre-release design phase.

Current source-of-truth documents are:

- `ARCHITECTURE.md`
- `QUICKSTART.md`
- `MODEIO_PLUGIN_PROTOCOL.md`
- `REFACTOR_GUIDE.md`

The earlier draft content was intentionally removed because it described an older chat-only, non-streaming gateway shape that no longer matches the shipped middleware.
